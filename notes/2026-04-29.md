# 개발 일지 — 2026년 4월 29일

> 이전 작업: [2026-04-28.md](./2026-04-28.md) 참고 (메뉴 원가 기획)

## 작업 개요
4/28 plan을 실제 코드로 구현. 엑셀 v3~v5 정리, 점장 입력 가이드, DB 5테이블, 업로드 파이프라인, 원가 계산, `/recipes` 페이지, 사이드바·UploadSection 통합 — 한 번에 라이브.
점장 미팅 전이라 데이터는 0이지만 **부분 입력으로도 동작**하도록 설계해서 내일 주간보고용 데모 가능 상태.

---

## 1. 엑셀 템플릿 v3 → v4 → v5 정리

### v3 — 결제방식 컬럼 추가
- 시트1에 `결제방식` 컬럼 (현금/카드) — 4번 (재료 회수율) 화면에서 두 그룹 분리 표시용
- 현금 4종: 원두·우유·말차파우더·잠봉뵈르햄 (4/25 SUPPLIERS와 일치)
- README에 1·2·3·4 계산 흐름 그림 추가

### v4 — 누락 재료 보강
점장 피드백 반영:
- TWG 티백 3종 분리 (레이디그레이/캐모마일/페퍼민트 — 종류별 단가 다름)
- 팔레데떼 티백 추가
- 드립백·디저트 모두 수제 (점장: "원두 갈아서 포장까지", "디저트는 다 수제. 크로와상 붕어빵만 생지 사옴")
- 봄딸기주스 = 딸기 직접 갈아서 (수제재료 X, 메뉴 레시피에 직접)
- 핸드드립 원두 별도 (다른 원두)
- 구움과자세트 = 초코쿠키 4 + 마들렌 1 + 스콘 1
- 시트1 하단 분홍색 "자유 추가" 영역 (단가 수식 미리 들어감)

### v5 — 점장 입력 가이드 강화
사용자 피드백: "이대로 보내면 압도당함. 보강 필요"
- README 맨 위에 **"받으면 이렇게 시작하세요"** 5단계 (예상 1.5~2시간)
- ❓ 두 색상으로 분리:
  - 🟡 진노랑 = ★필수 (잔당 사용량/산출량/가격)
  - ⬜ 연회색 = 선택 (정책 확인·이름 매칭 메타)
- 자몽청·말차크림 **샘플 가짜 숫자** 옅은 파랑 + 회색 이탤릭 + "📌 예시" 메모
- 시트3 "사용량" 컬럼 옆 비고에 단위(g/펌프/회) 명시 — "단위·비고"로 컬럼명 변경

### 산출물
- `notes/레시피_템플릿_v5.xlsx` (23KB, 4시트 + README)
- 생성 스크립트 `notes/_make_recipe_template_v5.py`
- 테스트용 `notes/레시피_템플릿_v5_test_원두.xlsx` (원두만 채워서 업로드 확인용)

---

## 2. 점장 입력 가이드 — `docs/레시피-입력-가이드.md`

점장 PC의 Claude Code와 함께 쓸 수 있는 self-contained 가이드.

### 구성
1. 받으신 것 / 해야 하는 것 (한 줄)
2. 채우는 순서 5단계 — 시트별 구체 예시 (실제 표 포함)
3. **메뉴 원가 페이지 메시지 3종** — 단가/서브레시피/레시피 미등록의 차이 + 어느 시트 가서 뭘 채우나
4. 자주 막히는 케이스 Q&A 8개 — 새 재료 추가, 재료명 일치, HOT/ICE, 세트메뉴 등
5. Claude Code에게 물어보는 방법 — 실제 프롬프트 예시 (메시지별 트리거 포함)
6. 다 채우셨으면

### 메시지 3종 정리표
| 메시지 | 어느 시트 | 채우는 것 |
|---|---|---|
| ⚠ 우유 (단가 미등록) | 시트1 | 1세트용량/세트수/총가격 |
| ⚠ 딸기청 (서브레시피 미등록) | 시트2 | 1배치 산출량 + 구성재료 |
| 레시피 미등록 | 시트3 | 메뉴 행의 재료/사용량 |

→ 점장이 화면 보고 즉시 Claude한테 "이거 어디 채워?" 물어볼 수 있게.

---

## 3. DB 마이그레이션 — 5테이블 + 부속 2개

```sql
-- ingredients (재료 마스터) + ingredient_prices (이력)
-- recipes (메뉴 레시피, product_name_normalized 키)
-- sub_recipes + sub_recipe_items (수제재료 배합, 1단계)
-- packaging_sets + packaging_set_items (카테고리×온도)
```

설계 결정:
- **bigserial PK** (프로젝트 컨벤션). 4/28 plan은 uuid 였지만 다른 테이블과 일치시킴
- `payment_method` 컬럼 — 엑셀 결제방식 → DB 매핑
- `(ingredient_id, effective_date)` UNIQUE — 같은 날짜 단가 중복 방지 (재업로드 갱신)
- `packaging_sets`는 `serve_temp = NULL` 허용용 partial unique index (`COALESCE(serve_temp, '')`)
- `upload_history.file_type` CHECK 제거 (recipe 타입 추가 위함)

RLS 비활성화 (프로젝트 원칙).

---

## 4. 업로드 파이프라인 — `app/api/upload/recipe/route.ts`

### 단순화
4시트라 미리보기 단계 없이 **파싱 + 저장 일체형**. 결과 stats만 반환.

### 파서 핵심
- 단가는 (1세트용량 × 세트수)에서 **직접 재계산** — 엑셀 수식 캐시 무시 (생성 시 캐시 안 됨)
- ❓ / 빈 값 / `📌`·`예시` 메모 → 스킵 (안전)
- 메뉴명은 `stripVariantSuffix()`로 정규화 (HOT/ICE 통합 키)
- "↓ 점장 자유 추가" 마커 행 스킵

### 저장 순서
1. `ingredients` upsert (name 충돌)
2. `ingredient_prices` upsert (`ingredient_id, effective_date` 충돌)
3. `sub_recipes` upsert (`output_ingredient_id` 충돌) + items 전체 삭제 후 재삽입
4. `recipes` 메뉴 단위 삭제 후 재삽입 (구성 변경 반영)
5. `packaging_sets` 카테고리×온도로 조회/생성 + items 삭제 후 재삽입

### 부분 입력 처리
- 단가 누락이면 그 재료의 `ingredient_prices` 행 안 들어감
- 잔당 사용량 ❓이면 그 메뉴의 그 라인만 스킵
- 다음 업로드 시 같은 양식 그대로 → 갱신

---

## 5. 원가 계산 — `lib/supabase/queries/recipe-costs.ts`

### `MenuCost` 타입
```ts
type CostStatus = 'ok' | 'no_recipe' | 'missing_price'

interface MenuCost {
  productId, productName, productCategory, servingTemp, price
  status, baseCost?, packagingCost?, totalCost?, margin?, costRatio?
  missingIngredients?: string[]
}
```

### 계산 흐름
```
products(active) → 각 메뉴
  ├─ recipes 조회 by stripVariantSuffix(name)
  │   ├─ 없으면 no_recipe
  │   └─ 있으면 각 ingredient_id로 단가 조회
  │       - kind='purchased': ingredient_prices 시점별 최신
  │       - kind='made': sub_recipes 재귀 (1단계) — 각 구성재료 × 단가 / output_quantity
  ├─ packaging_sets by (category, temp) — 매칭 없으면 0원 (실패 X)
  └─ 누락 1개라도 있으면 missing_price + missingIngredients 리스트
```

### 카테고리 매핑
products 테이블에 카테고리 컬럼 없음 → `daily_sales`에서 distinct (`stripVariantSuffix(name)` → category)로 빌드.

### `summarizeCosts(costs)`
- 평균 원가율 = `ok` 메뉴만으로 (미등록은 0원 처리 X — 평균 왜곡 방지)

---

## 6. `/recipes` 페이지

### 구성
- PageTabs (settlement 그룹 — 손익/지출/메뉴/**원가**)
- 첫 줄 요약: 전체 N개 / ✓ 원가 등록 / ⚠ 단가 누락 / 레시피 미등록 / 평균 원가율
- 표: 메뉴/분류/판매가/원가/마진/원가율
  - 필터: 전체 / 원가 등록 / 미등록
  - 정렬: 원가율 높은순 / 마진 적은순 / 이름순
  - 미등록 행 회색 메모로 "⚠ 우유 (단가 미등록) 외 N건"

### 원가율 색상
- < 30% 초록 (안전)
- 30-40% 주황 (주의)
- ≥ 40% 빨강 (위험)

---

## 7. 사이드바·PageTabs·UploadSection 통합

- 사이드바 결산 그룹에 `/recipes` 포함
- PageTabs settlement에 "원가" 탭 추가
- UploadSection 4번째 탭 "🥄 레시피·원가" — 일별/통장/메뉴 옆
  - 다른 탭들(파일→파싱→미리보기→저장)과 달리 한 번에 처리
  - 결과 stats: 재료 N개(단가 N) / 서브레시피 N종 / 메뉴 N / 포장세트 N

---

## 8. CLAUDE.md 보강

- **점장용 가이드 포인터** 섹션 추가 (`docs/점장-가이드.md`, `docs/레시피-입력-가이드.md`)
- **메뉴 원가 데이터 모델** 섹션 추가 (5테이블 스키마 요약)
- **현재 작업 단계** 4/27 → 4/29 갱신 (메뉴 원가 파이프라인 완료, 다음 작업 후보 정리)

→ 점장 PC의 Claude Code가 "claude.md + 노트 보고 + 레시피 입력 방법 알려줘" 식 프롬프트에 깔끔하게 답할 수 있는 상태.

---

## 9. 첫 업로드 검증 (사용자 테스트)

원두 1봉(1kg, 35,000원) 단가만 채운 v5 파일로 업로드:
- 반영 결과: 재료 79개(단가 1) / 서브레시피 1종(흑임자크림) / 메뉴 50/100라인 / 포장세트 9
- `/recipes` 결과:
  - 에스프레소 4,000 − 18g×35원 = **마진 3,370원, 원가율 15.8%** ✓
  - 아메리카노 동일 ✓
  - 카페라떼·라떼류: ⚠ 우유 (단가 미등록)
  - 핸드드립: ⚠ 핸드드립 원두 (단가 미등록)
  - 더치커피류: 레시피 미등록 (사용량 ❓)
  - 말차류: ⚠ 말차파우더, 순수 (단가 미등록)

→ 의도대로 동작. 우유 단가만 추가하면 라떼·카페모카·딸기라떼 등이 줄줄이 활성화되는 구조.

---

## 10. 1봉 vs 10봉 단가 동치성 (대화 메모)

점장이 "한 번 살 때 보통 10봉" 이라 했고 4월 통장엔 80만 찍힘.
- 1봉 35,000원 / 1세트용량 1000 / 세트수 1 = 35원/g
- 10봉 350,000원 / 1세트용량 1000 / 세트수 10 = 35원/g
- **단가 계산은 완전 동일**, 미래 "봉지당 회수율" 화면도 영향 없음 (1봉 = 1세트용량 × 단가)
- "한 번 매입 시 N봉" 정보는 세트수에 들어가는데 정확히 모르면 1로 두는 게 깔끔. 점장 컨펌 후 갱신.

---

## 11. 발생했던 이슈

| 이슈 | 원인 | 해결 |
|---|---|---|
| 업로드 후 "탭이 안 보임" | dev 서버 hot reload 못 잡음 | 사용자 새로고침으로 해결 |
| 샘플 가짜 숫자가 DB 들어갈 위험 | `📌`·`예시` 메모 자동 스킵 + 테스트 파일에선 ❓로 비움 | 이중 안전 |
| Python 환경: `python` 안 되고 `py` 만 있음 | Python launcher만 설치됨 | `py -m pip install openpyxl` 후 진행 |

---

## 12. 느낀 점

- **부분 입력 설계의 가치**: 점장 데이터 0% 상태에서도 시스템이 "0원 처리 X / missing_price 명시" 로 깨끗이 동작. 평균 집계가 ok 메뉴만으로 계산되니 부분적이라도 신뢰할 수 있는 숫자가 나옴. 이 설계가 4/28 plan §3.7에서 이미 정해진 게 결정적.

- **계획 → 구현이 거의 자동**: 4/28의 plan 파일 + v5 엑셀 양식이 진실의 원천이 되어 코드 작업이 매끄러웠음. 5테이블 스키마/파서 흐름/원가 계산식이 모두 plan에 적혀있어 "구현 = 옮기기"에 가까움. 4/25 IA 대수술과 같은 패턴 — *합의·문서화 → 구현은 거의 자동*.

- **점장 PC의 Claude Code = 미지원 부분의 백업**: 시스템이 입력 양식만 받고 직접 도와주진 못함. 그 빈 공간을 점장 PC의 Claude Code가 메움 — 가이드 파일 self-contained로 만들고 CLAUDE.md에 포인터 두니, 점장이 막힐 때마다 즉답 가능. *문서가 곧 백업 시스템*.

- **메시지 분류의 위력**: `no_recipe / missing_price / ok` 세 상태 + missingIngredients 리스트만으로 점장이 "왜 이거 안 떴어? → 어디 가서 채우면 돼" 까지 자기 해결 가능. 화면이 자체적으로 다음 행동을 가리키는 구조.

- **샘플 가짜 숫자 = 양날의 검**: v5에서 자몽청·말차크림 샘플로 패턴 학습 도와주려고 추가했는데, *실데이터로 오인될 위험* 있음. 메모 마커(`📌`)로 파서 자동 스킵 + 테스트 파일에선 ❓로 비움 — 이중 안전. 하지만 점장이 실제 입력하면서 메모 지우고 숫자만 두는 케이스는 여전히 위험. 추후 입력 끝난 v5 받으면 검증 필요.

- **단가 계산의 단위 불변성**: "1봉 vs 10봉" 케이스에서 결과 동일. 단가 = 총가격/(용량×세트수)이라 단위 단위가 무엇이든 결과는 같음. 이게 양식 설계의 견고함.

- **테스트 파일을 만들고 직접 검증한 가치**: 원두 1봉만 채운 테스트 파일로 검증해보니 의도대로 동작. 빈 시스템 → 부분 데이터 → 결과 검증의 사이클이 빨라야 다음 단계 자신감 ↑.

---

## 13. 미완료 / 다음 작업

### 점장 데이터 도착 후
- [ ] 점장이 v5 채워서 업로드 (오늘 안에 매입재료 가격 + 핵심 메뉴 5~10개만 있어도 데모 OK)
- [ ] 입력 검증 → 파서 미세 조정 (단위 환산 엣지케이스 발견 시)
- [ ] `CostRatioCards` / `DeficitSignals` 원가율 pending 해제 (대부분 메뉴 ok 상태 후)

### 다음 화면 (3·4번)
- [ ] 재료 1세트 회수율 화면 (실제 mix 기반)
  - 일평균 사용량 = Σ(메뉴별 일판매잔수 × 잔당 사용량)
  - 봉지 며칠치 = 1세트 용량 ÷ 일평균
  - N일치 마진 합 vs 봉지 가격 → 회수율
- [ ] 월 단위 재료비 진단
  - 현금 4종 (counterpart 추적): "4월 원두 80만 → 추정 사용 65만"
  - 카드 묶음: "4월 카드 재료비 vs 매출 추정 사용분"

### 작은 개선
- [ ] 점장이 입력하다가 양식 흔들면 (새 컬럼 필요 등) 파서 미세 조정
- [ ] /recipes 페이지에 메뉴별 클릭 → 상세 (어떤 재료 얼마, 어떤 포장)

---

## 14. 오후 — 마진 분해(/profit) + 회수율 재설계 + 80% 임계값

오전 commit(`8aec743`) 이후 점장 입장에서 본 사용 흐름 점검 → 큰 IA·계산 방식 변경.

### 14-1. 사용자가 만든 4번 → 잘못된 방향 (재고 진단)
처음 만든 `/expenses` 첫 줄의 "재료비 진단"은 매입 vs 추정 사용분 비교 (재고 관리 느낌).
사용자: "재고 말한 거 아니라 마진으로 얼마 벌고 있냐를 본 거였음".
→ 화면 통째로 폐기. `MaterialDiagnosis` 컴포넌트, `material-diagnosis.ts` 삭제.

### 14-2. 점장 입장 시뮬레이션 → IA 안 흔들고 /profit 강화로
처음 사용자 제안: 사이드바 새 그룹 "원가/마진".
점장 사용 흐름 다시 보면 사장 보고는 `/profit`에서 일어남. 마진 분해 = 손익 분석의 본질이라 *별도 페이지가 아니라 /profit 강화*가 자연.
→ IA 4그룹 안 만듦. 결산 그룹 그대로. /profit + /recipes 강화로.

### 14-3. /profit 마진 분해
- `getYearlyMaterialMetrics(year)` — 월별 추정 자재비 + 등록 메뉴 매출 (`material-cost.ts`)
- 수제재료를 매입재료로 분해 (`expandToPurchased`) — 흑임자크림 80g → 흑임자 20 + 생크림 60
- `MarginBreakdown` 컴포넌트 — 매출 → 자재비 → 그로스 마진 → 그 외 비용 → 순이익 막대 시각화
- YTD KPI에 그로스 마진 카드 추가 (손익분기 카드 대체)
- 누적 요약 바에 자재비 라인
- `MonthlyBreakdownTable`에 자재비 컬럼 (조건부)

### 14-4. /recipes sub-tab 2개
- `RecipesTabs` 컴포넌트 — 메뉴 원가 / 재료 회수율 두 탭
- `IngredientRecoveryTable` 통째 재설계:
  - 이전: 최근 30일 평균 사용량 기반
  - 변경: **마지막 매입 거래 단위** — `monthly_expenses.counterpart` 으로 마지막 거래일 찾고, 그 후 매출 기반 진행률·회수율
  - 현금 4종(원두·우유·말차파우더·잠봉뵈르햄)만. 카드 결제는 시점 추적 불가
- 회수율 표 컬럼: 재료 / 마지막 매입 / 매입량 / 경과 / 진행률 / 누적 마진 / 회수율
- 진행률 색 (60%+ 주황, 90%+ 빨강) — 발주 타이밍 시그널

### 14-5. 80% 신뢰도 임계값 — 핵심 결정
첫 번째 안: 50% — 사용자: "50%면 의미 있나?"
실제 따져보면:
- 50% 등록 시 마진율 오차 ±7.5%p (등록·미등록 메뉴 원가율 25% vs 40% 가정)
- 70%: ±4.5%p / 80%: ±3%p
→ 50%는 *그럴듯한 거짓말* 위험. 사장한테 "원가율 25%" 보고했는데 실제 32%면 치명적.
→ **80%로 통일**. /profit 마진 분해 + /recipes 회수율 둘 다.
임계값 미달 시 **잠금 카드** + "현재 등록 비중 X% / 임계값 80%" 메시지.
이유: 점진적 채움 시각화 + 점장 동기 + 80% 도달은 메인 메뉴 28개 채우면 가능.

### 14-6. 점장 가이드 — "어떤 메뉴부터 채우면 효율?" 추가
DB의 매출 상위 메뉴를 추출해 가이드 맨 위에 표시:
- `notes/_fetch_top_menus.py` 신설 — 최근 3개월(02-04) POS 데이터에서 매출 상위 메뉴 추출
- HOT/ICE 통합 기준 **상위 28개 메뉴 = 매출의 80.7%** 커버
- 카테고리별 묶어서 가이드에 표시 (커피·라떼 8 / 말차 4 / 디저트 10 / 음료·티 6)
- 필수 매입재료 + 필수 수제재료 리스트도 함께
- → 점장이 "1.5~2시간 동안 어디부터 채울지" 명확

---

## 15. 산출물 (4/29 오후)

```
lib/supabase/queries/material-cost.ts             ← 추정 자재비 + 등록 매출 비중
lib/supabase/queries/recipe-costs.ts (수정)       ← getIngredientRecovery 재설계
components/profit/MarginBreakdown.tsx             ← 매출 → 자재비 → 그로스 → 순이익 분해
components/profit/ProfitStatCards.tsx (수정)      ← 그로스 마진 카드 조건부
components/profit/MonthlyBreakdownTable.tsx (수정) ← 자재비 컬럼 조건부
components/recipes/IngredientRecoveryTable.tsx (수정) ← 매입 거래 기반
components/recipes/RecipesTabs.tsx                ← sub-tab 컨테이너
app/profit/page.tsx (수정)                        ← 80% 임계값
app/recipes/page.tsx (수정)                       ← sub-tab + 80% 임계값
notes/_fetch_top_menus.py                         ← 매출 상위 메뉴 추출 스크립트
notes/_top_menus.json                             ← 결과 (Top 28)
docs/레시피-입력-가이드.md (수정)                  ← '★ 가장 중요' 섹션 추가

삭제:
components/expenses/MaterialDiagnosis.tsx         ← 잘못된 방향
lib/supabase/queries/material-diagnosis.ts        ← 잘못된 방향
```

---

## 16. 느낀 점 (오후 추가)

- **사용자가 만든 4번 다시 들어보기**: "마진으로 얼마 벌고 있나" 라는 단순한 표현이 정확히 무엇인지 처음엔 *재고 진단*으로 잘못 해석. 한 번 더 묻는 게 맞았음. 만들기 전에 1-2 옵션 명확히 합의하는 패턴이 헛작업 줄임.

- **사이드바 4그룹 vs 페이지 강화**: 사용자가 "사이드바에 새 항목 만들자" 제안 → 점장 입장 다시 보고 IA 안 흔드는 게 본질에 맞다 판단. *사용자 제안을 충분히 들었으면 더 좋은 방향 제시도 OK*. 거스르되 이유 명확하게.

- **임계값 50% → 80% 조정**: 신뢰도 = 등록률의 함수라는 걸 *수치로 따져보니* 50%는 거짓말 위험. 사용자가 "50%면 의미 있나?" 직관적 의문 제기 → 정량 분석으로 확인 → 80% 도출. *임계값은 직관과 수치 둘 다 맞아야 안전*.

- **잠금 카드 = 동기부여 + 정직성**: 자료 부족할 때 무리하게 숫자 보여주는 것보다, 잠금 + 진행률 표시가 점장에게 *다음 액션* 명확. 진행률 X% / 임계값 80%가 한눈에. "라떼 단가 채우면 활성화" 같은 후속 동기.

- **재료 회수율 = 마지막 매입 거래 단위**: 이전 "최근 30일 평균"은 추상적이었음. *원두 4/15에 10봉 35만 → 14일 됐고 67% 사용했고 마진 56만 회수*는 점장이 즉시 행동 연결되는 정보. *집계 단위가 사용자 사고 단위와 일치해야 의미*.

- **매출 80% 커버 메뉴 = 입력 효율의 정답**: 가이드에 "상위 28개" 명시 → 점장이 1.5~2시간 안에 핵심 데이터 다 채울 수 있게 됨. *파레토 법칙은 데이터 입력에도 적용*. "전체 다 채워야"라는 부담을 "80%만 먼저"로 바꾸니 진입 장벽 낮음.

- **DB 직접 쿼리 패턴 다시 활용**: 4/28에 `_fetch_menu_data.py` 만들었던 패턴 그대로 재사용해서 `_fetch_top_menus.py` 30분 안에 작성·실행. 데이터 기반 결정이 즉시 가능한 환경 = 가설을 빨리 검증.

---

## 17. 다음 작업 (이전 항목 갱신)

### 점장 데이터 도착 후
- [ ] 점장이 v5 채워서 업로드 (특히 상위 28개 메뉴) — 회수율·마진 분해 활성화 확인
- [ ] 입력 검증 → 파서 미세 조정
- [ ] CostRatioCards / DeficitSignals 원가율 pending 해제 (등록률 80%+ 기준)

### 데이터 충분히 쌓인 후 검토
- [ ] 80% 임계값 적정성 (실제 사용해보고 점장 의견)
- [ ] 회수율 화면에 카드 결제 재료 표시 옵션 (월 단위 합계만이라도)
- [ ] 신호등 절대 임계값 (운영 6개월)

---

## 18. 산출물 목록 (전체 — 오전+오후)

```
docs/레시피-입력-가이드.md          ← 점장 입력 가이드
notes/레시피_템플릿_v3.xlsx         ← 결제방식 추가
notes/레시피_템플릿_v4.xlsx         ← 누락 재료 보강
notes/레시피_템플릿_v5.xlsx         ← 5단계 가이드 + 색 분리 + 샘플 (점장 전달용)
notes/레시피_템플릿_v5_test_원두.xlsx ← 테스트 검증용
notes/_make_recipe_template_v3~v5.py
notes/_make_test_template.py

app/api/upload/recipe/route.ts      ← 파싱 + 저장 일체형
lib/supabase/queries/recipe-costs.ts ← 원가 계산 read 함수
app/recipes/page.tsx                ← 메뉴별 원가 페이지
components/recipes/CostTable.tsx    ← 표 컴포넌트

components/ui/Sidebar.tsx           ← /recipes 결산 그룹 추가
components/ui/PageTabs.tsx          ← settlement에 '원가' 탭
components/upload/UploadSection.tsx ← 4번째 탭 + RecipeUploadResult

CLAUDE.md                            ← 점장 가이드 포인터 + 메뉴 원가 모델 섹션
```

---

## 19. 저녁 — 모바일 업로드 + 통장 파싱 + 합계 일관성 (점장 실사용 버그 3개)

오후 commit(`8aec743`) 이후 점장이 폰으로 시스템 써보다가 연쇄로 발견된 버그들. 한 사이클로 해결.

### 19-1. 안드로이드 파일 선택 비활성화
**증상**: 안드로이드폰 `/upload` 진입 → 파일 선택 자체가 비활성화처럼 보임 (클릭해도 반응 없거나 파일이 회색).

**원인 두 가지**:
- `<div onClick={() => inputRef.current?.click()}>` 패턴이 안드로이드 인앱 브라우저(카톡/네이버앱)에서 막힘 — 프로그램적 click()이 사용자 제스처로 인정 안 됨
- `accept=".xlsx,.xls"` 확장자만 — 안드로이드 파일관리자는 MIME 기반 필터라 .xlsx 파일이 회색 처리

**해결** ([components/upload/FileDropZone.tsx](../components/upload/FileDropZone.tsx)):
- `<div>` → `<label>` 로 교체. 브라우저 native label-input 연결 사용 (useRef 제거)
- `augmentAccept()` — 확장자에 표준 MIME 자동 보강
- `loading` 중 `disabled` + cursor-not-allowed (덤)
- `e.target.value = ''` — 같은 파일 재선택 가능 (덤)

### 19-2. "파싱 중" 무한 표시
**증상**: 안드로이드에서 파일 선택 후 "파싱 중..." 만 계속 떠 있음.

**원인**: `handleFile`에 try/catch 없음 → fetch 실패하면 `setLoading(false)` 영원히 호출 안 됨.

**해결** ([components/upload/UploadSection.tsx](../components/upload/UploadSection.tsx)):
- try/catch/finally 감싸기. finally에서 `setLoading(false)`
- `safeJson` 헬퍼 — 서버가 502/504 HTML 반환 시 JSON.parse 실패로도 죽지 않게
- 에러 메시지에 status code 포함 → 모바일에서 원인 식별 가능

### 19-3. 통장 업로드 500 → 암호 걸린 .xls
**증상**: PC에서도 통장 업로드 실패. "파싱 실패 (500)".

**진단 단계**:
1. bank route에 try/catch + 시그니처 hex 노출 → 에러 메시지 점장 화면에 보이게
2. 실제 에러: `t.charCodeAt is not a function` — SheetJS가 진짜 xlsx 아닌 걸 받았을 때 시그니처
3. 시그니처 `D0 CF 11 E0` = CFB (구형 .xls 또는 암호 걸린 modern xlsx)
4. SheetJS 한 번 더: `File is password-protected`

**근본 원인**: 하나은행 다운로드 옵션의 **"암호 설정"** 이 켜져 있어서 export가 암호 걸린 CFB 파일로 나옴. 점장 생년월일 6자리(710320)가 고정 암호.

**해결** ([app/api/upload/bank/route.ts](../app/api/upload/bank/route.ts)):
- `xlsx-populate` 패키지 추가 (CFB-wrapped agile encryption 복호화 지원)
- 시그니처 감지 → CFB이면 `XlsxPopulate.fromDataAsync(buffer, { password })` 로 복호화 → 결과 buffer를 SheetJS로 전달
- 암호는 `BANK_FILE_PASSWORD` 환경변수 (기본값 `710320`, Vercel에서 override 가능)
- runtime 'nodejs' 강제 (xlsx-populate가 Buffer/crypto 사용)
- 복호화 실패 시(암호 안 걸린 .xls 케이스) SheetJS에 fallback. 그래도 실패하면 시그니처 hex + 에러 메시지 노출
- HTML 위장 .xlsx (`.xls` 확장자에 HTML 내용) 케이스도 함께 대응 — UTF-8/EUC-KR 둘 다 시도

### 19-4. 대시보드 vs /expenses 합계 불일치
**증상 두 가지**:
- 거래를 'excluded'로 재분류 → /expenses는 즉시 빠지지만 대시보드 월 지출은 그대로
- 수동 조정 지출 차감 → 대시보드는 빠지지만 /expenses는 그대로

**원인 — 두 페이지가 다른 source-of-truth**:
- 대시보드 "월 지출" = `monthly_summary.total_expense` (recalc 결과 — manual_adjustments 반영, 토글에 따라 excluded 포함/제외)
- /expenses 헤드라인 합계 = `monthly_expenses` 카테고리 직접 합산 (`labor + ingredients + fixed + equipment + card`, 항상 excluded 제외, manual_adjustments **미반영**)
- 게다가 `include_owner_personal` 토글이 ON이면 recalc가 excluded를 포함해서 → 'excluded'로 재분류해도 대시보드 total_expense 변동 없음

**해결**:
- [lib/supabase/recalc.ts](../lib/supabase/recalc.ts): `include_owner_personal` 분기 제거. **'excluded'는 항상 제외** 로 단일화. 토글 의미 사라짐 → 사용자 혼란 제거.
- [app/expenses/page.tsx](../app/expenses/page.tsx): 헤드라인 `합계` = `summary.total_expense` 사용. manual_adjustments 자동 반영. 카테고리 합과 차이 나면 "(카테고리 N만 ± 수동조정 M만)" 표기로 투명성 확보.
- [app/api/recalc/route.ts](../app/api/recalc/route.ts): 신규 — 모든 월 일괄 재계산 (정책 변경 후 stale 데이터 회복용).
- [components/settings/RecalcButton.tsx](../components/settings/RecalcButton.tsx): /settings에 "재계산" 버튼. 점장이 1회 누르면 일괄 적용.
- [app/settings/page.tsx](../app/settings/page.tsx): `OwnerToggle` 제거(파일은 보존, 페이지에서만 빼기), `RecalcButton` 추가.
- CLAUDE.md system_settings 섹션 갱신 (`include_owner_personal` 제거 안내).

### 19-5. 산출물 (저녁)
```
components/upload/FileDropZone.tsx          ← label + MIME 보강 + disabled
components/upload/UploadSection.tsx         ← try/catch + safeJson
app/api/upload/bank/route.ts                ← CFB 복호화 + HTML 폴백 + 시그니처 진단
package.json / package-lock.json            ← +xlsx-populate

lib/supabase/recalc.ts                      ← excluded 항상 제외 (토글 분기 제거)
app/expenses/page.tsx                       ← summary.total_expense + 차이 표기
app/api/recalc/route.ts                     ← 신규 (일괄 재계산 트리거)
components/settings/RecalcButton.tsx        ← 신규 (재계산 버튼)
app/settings/page.tsx                       ← OwnerToggle 제거, RecalcButton 추가
CLAUDE.md                                   ← system_settings 섹션 정리
```

---

## 20. 느낀 점 (저녁 추가)

- **"안 되는데요" 한 마디 → 3개 버그 연쇄 발견**: 점장이 안드로이드에서 업로드 안 된다고 한 마디 한 게 시작 → 파일 선택 → 파싱 무한 → 통장 암호 → 대시보드 합계 불일치까지 줄줄이. 실사용자 한 번 손에 쥐어주는 게 *내부 디버깅 100시간보다 강함*. 점장 미팅 전에 자체적으로 "폰에서 한 번 켜본다" 가 부족했던 게 명확.

- **디버깅 단서 노출이 곧 속도**: 통장 500 에러 — 처음엔 그냥 "파싱 실패"만 떴는데, status code → 시그니처 hex → 에러 message 순으로 매번 메시지 강화. 점장이 메시지 그대로 알려주는 것만으로 1단계씩 좁힘. *서버 사이드 에러를 클라이언트에 안전하게 노출하는 패턴*이 원격 디버깅의 핵심.

- **하나은행 암호 설정 = 표준 Office encryption**: 한국 은행 export가 종종 비표준일 거라 두려웠는데, 실제로는 표준 OOXML agile encryption. xlsx-populate 한 줄로 풀림. *국지적 보안 기능도 결국 글로벌 표준 위에 있을 때가 많음*. 비표준 케이스(HTML 위장, EUC-KR 등)는 fallback으로 두고 표준 먼저 시도.

- **단일 source-of-truth = 일관성의 본질**: 대시보드/expenses 합계 불일치 — 양쪽이 *다른 계산 경로*를 가지고 있던 게 근본. 한쪽은 recalc'd `total_expense`, 한쪽은 카테고리 직접 합산. 두 경로 자체가 *드리프트의 원인*. → 둘 다 같은 함수/필드 보게 통일. *"기능을 추가" 보다 "두 페이지가 같은 진실을 보게"가 우선*.

- **토글 제거 = 사용자 혼란 해결**: `include_owner_personal` 토글이 사용자가 의식 못 하는 사이에 결과 바꾸고 있었음. 사용자 의도("excluded는 항상 제외")와 토글 디자인("excluded 포함 여부 선택")이 어긋남 → 토글 자체가 노이즈. 깔끔히 제거. *잘 안 쓰이는 설정은 빼는 게 사용자에게 친절*.

- **"재계산" 버튼 = 회복 가능한 시스템**: 정책 변경 후 stale 데이터 처리 → 자동 마이그레이션 대신 *사용자가 누르는 1회성 버튼*. 자동화 안 해도 됨, *명시적 액션이 추적 가능*. 향후 비슷한 일관성 정정 작업도 같은 패턴 재사용 가능.

- **모바일 호환성 디테일 5종**: label vs onClick / MIME accept / disabled feedback / 같은 파일 재선택 / try-catch — 데스크탑에선 다 noop이지만 모바일에선 각각 막힘. *데스크탑 위주로 빌드 → 모바일에서 깨짐*은 흔한 패턴. 점장처럼 모바일 사용자 있으면 처음부터 모바일 검증 사이클 들어가야.

---

## 21. 다음 작업 (갱신)

### 점장 데이터 도착 후
- [ ] 점장 폰에서 통장 업로드 실제 성공 확인 (암호 걸린 파일 그대로)
- [ ] `/settings` "재계산" 버튼 1회 클릭 → 두 페이지 합계 일치 확인
- [ ] 점장이 v5 채워서 업로드 (특히 상위 28개 메뉴) — 회수율·마진 분해 활성화 확인
- [ ] 입력 검증 → 파서 미세 조정
- [ ] CostRatioCards / DeficitSignals 원가율 pending 해제 (등록률 80%+ 기준)

### 데이터 충분히 쌓인 후 검토
- [ ] 80% 임계값 적정성 (실제 사용해보고 점장 의견)
- [ ] 회수율 화면에 카드 결제 재료 표시 옵션 (월 단위 합계만이라도)
- [ ] 신호등 절대 임계값 (운영 6개월)
- [ ] /expenses 카테고리 breakdown에 "수동조정" 라인 별도 표시 옵션

### 잠재 후속
- [ ] OwnerToggle / getIncludeOwnerPersonal 완전 제거 (현재 dead code 상태)
- [ ] 다른 모바일 미체크 페이지 점검 (/profit, /recipes, /menu)

---

## 기술 스택 현황 (변동: xlsx-populate 추가)
- Next.js 16.2.4 (App Router) + TypeScript + Tailwind CSS v4
- Supabase (PostgreSQL, RLS 비활성화)
- SheetJS (xlsx), **xlsx-populate** (암호 해제용), Recharts
- 엑셀 생성: openpyxl (Python 3.14)
